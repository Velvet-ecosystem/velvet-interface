# Example surface assets

Place the real background image referenced by a surface manifest in this directory, or change the manifest to point at its actual repository or deployment path.

For the included Founder example, the expected file is:

```text
examples/assets/founder_home.png
```

No placeholder image is included. A missing image must remain visibly unavailable rather than being replaced with synthetic artwork.
